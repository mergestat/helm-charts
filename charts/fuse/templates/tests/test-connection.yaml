apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fuse.fullname" . }}-test-connection"
  labels:
    {{- include "fuse.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fuse.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
